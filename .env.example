# Shared password for the host-only /simulator page.
# Copy this file to .env.local and set a real value, then restart `npm run dev`.
# On Vercel, add SIMULATOR_PASSWORD under Project Settings → Environment Variables.
SIMULATOR_PASSWORD=change-me
